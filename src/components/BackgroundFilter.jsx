const BackgroundFilter = ({ handleModalClose }) => (
    <div onClick={handleModalClose} className='fixed top-0 left-0 w-[100vw] h-[100vh] bg-black bg-opacity-40 z-40'></div>
)

export default BackgroundFilter
